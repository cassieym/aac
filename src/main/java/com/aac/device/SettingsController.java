package com.aac.device;

import javafx.stage.Stage;

public class SettingsController {
    public void start(Stage stage) throws Exception {
//        ListView<HBox> listView = new ListView<>();
//        ObservableList<HBox> items = FXCollections.observableArrayList();  // list so data changes auto update in GUI
//
//        for(CategoryGroup categoryGroups : MainComponent.categoryGroups) {
//            for (Category categories : Category)
//            HBox blockKey = new HBox(new Label("Category " + i));
//            items.add(blockKey);
//        }
//        listView.setItems(items);
    }
}
